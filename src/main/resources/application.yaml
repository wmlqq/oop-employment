spring:
  application:
    name: employment
  datasource:
    # 替换为你的数据库名（需提前在MySQL中创建）
    url: jdbc:mysql://localhost:3306/employment_consultation?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root  # 你的MySQL用户名（默认通常是root）
    password: 3141306947w666W@  # 你的MySQL密码
    driver-class-name: com.mysql.cj.jdbc.Driver  # MySQL 8+ 驱动类名
  jpa:
    hibernate:
      ddl-auto: update  # 自动根据实体类更新数据库表结构
    show-sql: true      # 在控制台显示执行的 SQL 语句
    properties:
      hibernate:
        format_sql: true # 格式化 SQL 输出，便于阅读